# 152-FZ Compliance as Code

![CI](https://github.com/ranas-mukminov/152fz-compliance-as-code/actions/workflows/ci.yml/badge.svg)
![Security](https://github.com/ranas-mukminov/152fz-compliance-as-code/actions/workflows/security.yml/badge.svg)
![License](https://img.shields.io/badge/license-Apache%202.0-blue)
![Python](https://img.shields.io/badge/python-3.10%2B-blue)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

**Автор и сайт:** [Ranas Mukminov](https://github.com/ranas-mukminov) | [run-as-daemon.ru](https://run-as-daemon.ru)

## Описание

**152-FZ Compliance as Code** — это инструментарий с открытым исходным кодом на Python, который позволяет организациям управлять соответствием требованиям Федерального закона № 152-ФЗ «О персональных данных» через декларативную YAML-конфигурацию. Вместо ручного ведения таблиц и документов Word вы один раз описываете процессы обработки данных в структурированном формате и автоматически генерируете реестры, формы согласий, поручения обработчикам, диаграммы потоков данных и отчёты по рискам в стиле DPIA. Этот проект предназначен для малого и среднего бизнеса, интеграторов, аудиторов и DevSecOps-команд, которые хотят внедрить практики «Compliance as Code».

## Основные возможности

- **Декларативный YAML DSL**: Определение операторов, субъектов данных, категорий персональных данных и процессов обработки в версионируемом конфигурационном файле.
- **Автоматическая генерация документов**: Создание Реестра процессов обработки, форм согласий и поручений обработчикам (DPA) в формате DOCX.
- **Диаграммы потоков данных**: Автоматическое построение визуальных схем движения данных с использованием Mermaid или PlantUML.
- **Движок оценки рисков**: Встроенный DPIA-лайт риск-скоринг для выявления процессов с высоким риском.
- **CLI и Web UI**: Инструмент командной строки (`pd152`) для интеграции в CI/CD и веб-мастер на базе FastAPI для интерактивной настройки.
- **Готовность к CI/CD**: Валидация конфигураций и генерация артефактов как часть автоматизированных пайплайнов.
- **Расширяемые шаблоны**: Шаблонизация на основе Jinja2 для простой кастомизации генерируемых документов.
- **Privacy-First**: Реальные персональные данные не требуются—используйте анонимизированные примеры и заглушки.

## Архитектура и компоненты

Инструментарий состоит из нескольких модульных компонентов:

```
┌─────────────────────────────────────────────────────────────┐
│                   Пользовательский ввод                     │
│  ┌──────────────────┐         ┌─────────────────────────┐  │
│  │  YAML Config     │         │  Web Wizard (FastAPI)   │  │
│  │  (pd-config.yaml)│         │  http://localhost:8000  │  │
│  └─────────┬────────┘         └───────────┬─────────────┘  │
└────────────┼───────────────────────────────┼────────────────┘
             │                               │
             ▼                               ▼
┌─────────────────────────────────────────────────────────────┐
│             YAML Loader & Validator                         │
│  - Валидация по JSON Schema                                 │
│  - Маппинг в доменные модели (models.py)                    │
└─────────────────────┬───────────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┬─────────────────┐
        │             │             │                 │
        ▼             ▼             ▼                 ▼
┌──────────────┐ ┌──────────┐ ┌──────────────┐ ┌──────────┐
│ Register     │ │ Consents │ │ Data Flow    │ │ Risk     │
│ Generator    │ │ Generator│ │ Diagrams     │ │ Engine   │
│ (DOCX)       │ │ (DOCX)   │ │ (Mermaid)    │ │ (DPIA)   │
└──────┬───────┘ └────┬─────┘ └──────┬───────┘ └────┬─────┘
       │              │              │              │
       └──────────────┴──────────┬───┴──────────────┘
                                 │
                                 ▼
                      ┌──────────────────────┐
                      │  Выходные артефакты  │
                      │  - register.docx     │
                      │  - consents.docx     │
                      │  - flows.mmd         │
                      │  - risks.txt         │
                      └──────────────────────┘
```

**Ключевые компоненты:**
- **Доменные модели**: Python dataclasses, определяющие операторов, субъектов данных, процессы обработки и т.д.
- **YAML Schema**: Валидация на основе JSON Schema для конфигурационного DSL.
- **Генераторы**: Модульные генераторы для различных типов документов (DOCX, диаграммы, отчёты).
- **Risk Engine**: Скоринг рисков на основе чувствительности данных, трансграничных передач и мер безопасности.
- **CLI (`pd152`)**: Интерфейс командной строки на базе Typer для всех операций.
- **Web App**: Веб-мастер на FastAPI для создания конфигураций с подсказками.

## Требования

### Операционная система
- **Linux**: Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky Linux, Alma Linux
- **macOS**: 11+ (Big Sur и выше)
- **Windows**: 10/11 с установленным Python

### Программное обеспечение
- **Python**: 3.10 или выше (рекомендуется 3.11)
- **pip**: Последняя версия
- **Git**: Для клонирования репозитория

### Системные ресурсы
- **CPU**: 1 ядро (рекомендуется 2+)
- **RAM**: 512 МБ минимум (рекомендуется 1 ГБ)
- **Диск**: 100 МБ для приложения + место для генерируемых документов

### Сеть
- Доступ в Интернет для установки зависимостей Python из PyPI

## Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ranas-mukminov/152fz-compliance-as-code.git
   cd 152fz-compliance-as-code
   ```

2. **Установите пакет:**
   ```bash
   pip install .
   ```

3. **Проверьте пример конфигурации:**
   ```bash
   pd152 validate --config src/pd152_compliance_as_code/yaml_schema/examples/simple_online_shop.yaml
   ```

4. **Сгенерируйте все документы:**
   ```bash
   pd152 generate-all --config src/pd152_compliance_as_code/yaml_schema/examples/simple_online_shop.yaml --output ./artifacts
   ```

5. **Просмотрите сгенерированные файлы:**
   ```bash
   ls -lh artifacts/
   ```

## Установка

### Стандартная установка (для продакшена)

Для большинства пользователей, которые хотят использовать инструментарий как есть:

```bash
# Установка из исходников
git clone https://github.com/ranas-mukminov/152fz-compliance-as-code.git
cd 152fz-compliance-as-code
pip install .

# Проверка установки
pd152 --help
```

### Установка для разработки

Для контрибьюторов или тех, кто хочет модифицировать код:

```bash
# Клонирование и установка в editable режиме с dev-зависимостями
git clone https://github.com/ranas-mukminov/152fz-compliance-as-code.git
cd 152fz-compliance-as-code
pip install -e ".[dev]"

# Запуск тестов для проверки
pytest

# Запуск линтеров
scripts/lint.sh
```

### Системная установка (Ubuntu/Debian)

```bash
# Установка Python 3.10+ если не установлен
sudo apt update
sudo apt install python3.11 python3-pip git -y

# Клонирование и установка
git clone https://github.com/ranas-mukminov/152fz-compliance-as-code.git
cd 152fz-compliance-as-code
pip install .
```

### Системная установка (RHEL/Rocky/Alma)

```bash
# Установка Python 3.10+ если не установлен
sudo dnf install python3.11 python3-pip git -y

# Клонирование и установка
git clone https://github.com/ranas-mukminov/152fz-compliance-as-code.git
cd 152fz-compliance-as-code
pip install .
```

## Настройка

Основа инструментария — это конфигурационный файл YAML. Ниже приведен полный пример:

```yaml
operator:
  name: "ООО Ромашка Онлайн"
  inn: "1234567890"
  ogrn: "1234567890123"
  contacts:
    address: "г. Москва, ул. Цветочная, д. 1"
    email: "dpo@romashka.ru"
    phone: "+7 (495) 123-45-67"
    website: "https://romashka.ru"

processors:
  - name: "ООО Облачный Провайдер"
    role: "Провайдер облачного хостинга"
    contacts:
      email: "support@cloudprovider.ru"

data_subjects:
  - id: customers
    name: "Покупатели интернет-магазина"
    description: "Физические лица, приобретающие товары"
  
  - id: employees
    name: "Сотрудники компании"

personal_data_categories:
  - id: contact_data
    name: "Контактная информация"
    fields: ["ФИО", "Email", "Телефон", "Адрес доставки"]
    sensitivity: "standard"
  
  - id: payment_data
    name: "Платёжная информация"
    fields: ["Номер карты (замаскированный)", "История платежей"]
    sensitivity: "standard"

processing_activities:
  - id: order_processing
    name: "Обработка и исполнение заказов"
    subjects: ["customers"]
    data_categories: ["contact_data", "payment_data"]
    purposes:
      - "Исполнение договора купли-продажи"
      - "Координация доставки"
    legal_basis:
      - "Договор"
    operations:
      - "сбор"
      - "запись"
      - "хранение"
      - "передача"
    storage_locations:
      - "Сервер в РФ (ЦОД Москва)"
    recipients:
      - "Служба доставки (курьерская)"
      - "Платёжный процессор"
    retention:
      description: "5 лет для бухучёта и претензионной работы"
      period: "5 лет"
    security_measures:
      org:
        - "Ролевое разграничение доступа"
        - "Обучение сотрудников защите данных"
      tech:
        - "Шифрование на диске (AES-256)"
        - "Ежедневное резервное копирование"
        - "Логирование и мониторинг доступа"
      legal:
        - "Поручение обработчикам"
        - "Внутренняя политика защиты данных"
    notes: "Данные платёжных карт обрабатываются сертифицированным платёжным шлюзом, не хранятся в наших системах"
```

### Переменные окружения

В настоящее время инструментарий не требует переменных окружения для базовой работы. Вся конфигурация выполняется через YAML-файл.

## Использование

### Интерфейс командной строки

CLI `pd152` — это основной интерфейс для всех операций:

```bash
# Показать все доступные команды
pd152 --help

# Проверить конфигурационный файл
pd152 validate --config pd-config.yaml

# Сгенерировать все артефакты (реестр, согласия, диаграммы, отчёт по рискам)
pd152 generate-all --config pd-config.yaml --output ./out

# Сгенерировать только Реестр процессов обработки
pd152 generate-register --config pd-config.yaml --output ./out/register.docx

# Сгенерировать только формы согласий
pd152 generate-consents --config pd-config.yaml --output ./out/consents.docx

# Сгенерировать диаграммы потоков данных
pd152 generate-flows --config pd-config.yaml --output ./out/flows.mmd

# Сгенерировать поручения обработчикам (DPA)
pd152 generate-dpi --config pd-config.yaml --output ./out/dpa.docx

# Создать шаблон конфигурации
pd152 init-template --output my-config.yaml
```

### Веб-мастер

Для использования интерактивного веб-интерфейса:

```bash
# Запустить веб-сервер
uvicorn pd152_compliance_as_code.webapp.app:app --reload --host 0.0.0.0 --port 8000
```

Затем откройте браузер по адресу `http://localhost:8000` и используйте мастер для:
- Интерактивного создания новой конфигурации
- Валидации введённых данных в реальном времени
- Экспорта конфигурации в YAML

### Использование в CI/CD пайплайнах

Пример workflow для GitHub Actions:

```yaml
name: Compliance Docs

on:
  push:
    paths:
      - 'compliance/pd-config.yaml'

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install pd152
        run: pip install git+https://github.com/ranas-mukminov/152fz-compliance-as-code.git
      - name: Validate
        run: pd152 validate --config compliance/pd-config.yaml
      - name: Generate
        run: pd152 generate-all --config compliance/pd-config.yaml --output compliance/artifacts
      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: compliance-docs
          path: compliance/artifacts/
```

## Обновление

Для обновления до последней версии:

```bash
# Перейдите в локальный клон
cd 152fz-compliance-as-code

# Получите последние изменения
git pull origin main

# Переустановите
pip install --upgrade .

# Проверьте версию (информация о версии в CHANGELOG.md)
pd152 --help
```

**Критические изменения**: Всегда просматривайте `CHANGELOG.md` перед обновлением. Основные изменения в схеме YAML будут задокументированы там.

## Логи и устранение неполадок

### Ошибки валидации

Если `pd152 validate` завершается с ошибкой:
- Внимательно прочитайте сообщение об ошибке—оно укажет, какое поле отсутствует или некорректно.
- Проверьте синтаксис YAML (отступы, двоеточия, кавычки).
- Сравните вашу конфигурацию с примерами в `src/pd152_compliance_as_code/yaml_schema/examples/`.

**Типичные ошибки:**
```bash
# Неправильный синтаксис YAML
Error: mapping values are not allowed here
  ▶ Исправление: Проверьте отсутствующие двоеточия или неправильные отступы

# Отсутствует обязательное поле
Error: 'operator.name' is a required property
  ▶ Исправление: Добавьте имя оператора в ваш YAML

# Неверная ссылка
Error: Subject ID 'customer' not found in data_subjects
  ▶ Исправление: Убедитесь, что все ID, на которые ссылаются в processing_activities, существуют
```

### Проблемы с генерацией

Если генерация документов не работает:
- Убедитесь, что выходная директория существует или доступна для записи.
- Проверьте, что `python-docx` установлен: `pip install python-docx`
- Запустите с подробным выводом (если реализовано) или проверьте stderr.

### Проблемы с веб-интерфейсом

Если веб-мастер не запускается:
```bash
# Проверьте, не занят ли порт 8000
lsof -i :8000

# Используйте другой порт
uvicorn pd152_compliance_as_code.webapp.app:app --port 8080

# Проверьте установку uvicorn
pip install uvicorn[standard]
```

### Проблемы с производительностью

Для больших конфигураций (100+ процессов обработки):
- Генерация может занять 10-30 секунд.
- Используйте `scripts/perf_check.sh` для бенчмарка (если доступно).

## Замечания по безопасности

### Юридический отказ от ответственности

> **Этот инструмент НЕ заменяет профессиональную юридическую консультацию.** Генерируемые документы являются шаблонами и черновиками, которые ДОЛЖНЫ быть проверены квалифицированным юристом или ответственным за организацию обработки персональных данных (DPO) перед использованием в продакшене.

### Конфиденциальность данных

- **НЕ коммитьте реальные персональные данные** (имена, телефоны, email реальных людей) в ваши YAML-конфигурации.
- Используйте анонимизированные примеры: «Иван Иванов», «example@example.com», «+7 (XXX) XXX-XX-XX».
- Если храните конфигурации с чувствительной информацией, используйте приватные репозитории с контролем доступа.

### Управление секретами

- Данный инструмент в настоящее время не требует API-ключей или паролей.
- Если вы расширяете его внешними интеграциями (например, облачное хранилище), используйте переменные окружения или инструменты управления секретами, никогда не хардкодьте учётные данные.

### Внешние AI/LLM сервисы

Если вы используете внешние AI-сервисы для помощи в генерации YAML-конфигураций:
- Всегда анонимизируйте или псевдонимизируйте данные перед отправкой в сторонние сервисы.
- Не отправляйте реальные персональные данные в LLM без надлежащих правовых оснований и согласия субъектов данных.
- Проверяйте вывод AI—он может содержать ошибки или некорректные с точки зрения законодательства предложения.

### Контроль доступа

- Ограничьте доступ к веб-интерфейсу (`http://localhost:8000`) внутренними сетями или используйте аутентификацию (не включена по умолчанию).
- Не выставляйте веб-интерфейс напрямую в Интернет без обратного прокси с TLS и аутентификацией.

## Структура проекта

```
152fz-compliance-as-code/
├── src/
│   └── pd152_compliance_as_code/
│       ├── cli/                    # CLI команды (Typer)
│       ├── domain/                 # Модели данных (dataclasses)
│       ├── generators/             # Генераторы документов (DOCX, диаграммы)
│       ├── risk_engine/            # Логика оценки рисков
│       ├── webapp/                 # Web-приложение FastAPI
│       └── yaml_schema/            # YAML схема и примеры
├── tests/                          # Модульные и интеграционные тесты
│   ├── unit/                       # Модульные тесты
│   └── integration/                # Интеграционные тесты
├── scripts/                        # Скрипты для разработки и CI
│   ├── lint.sh                     # Запуск линтеров (ruff, mypy)
│   ├── security_scan.sh            # Запуск bandit и pip-audit
│   └── perf_check.sh               # Бенчмарки производительности
├── .github/
│   └── workflows/                  # GitHub Actions CI/CD
│       ├── ci.yml                  # Lint, test, генерация примеров
│       └── security.yml            # Security-сканирование
├── examples/                       # Примеры входных политик (markdown)
├── pyproject.toml                  # Конфигурация Python-проекта
├── LICENSE                         # Лицензия Apache 2.0
├── LEGAL.md                        # Юридические оговорки
├── CONTRIBUTING.md                 # Рекомендации для контрибьюторов
└── README.md                       # Данный файл
```

## Как внести вклад

Мы приветствуем вклад в развитие! Пожалуйста, следуйте этим рекомендациям:

1. **Откройте issue** перед началом работы над крупными изменениями.
2. **Сделайте форк репозитория** и создайте ветку для функции.
3. **Сосредоточьтесь на изменениях**: Одна функция или багфикс на PR.
4. **Добавьте тесты**: Покройте новый функционал модульными тестами.
5. **Запустите линтеры**: Убедитесь, что `scripts/lint.sh` проходит перед отправкой.
6. **Никаких реальных персональных данных**: Используйте только анонимизированные примеры.

**Настройка для разработки:**
```bash
pip install -e ".[dev]"
scripts/lint.sh
pytest
```

Подробные рекомендации см. в [CONTRIBUTING.md](CONTRIBUTING.md).

## Лицензия

Этот проект лицензирован на условиях **Apache License 2.0**. Полный текст см. в [LICENSE](LICENSE).

Вы можете свободно использовать, модифицировать и распространять это программное обеспечение, в том числе в коммерческих целях, при условии сохранения уведомления о лицензии и указания авторства.

## Автор и коммерческая поддержка

**Автор:** [Ranas Mukminov](https://github.com/ranas-mukminov)  
**Сайт:** [run-as-daemon.ru](https://run-as-daemon.ru)

Для внедрения в продакшене, кастомизации шаблонов, аудитов и постоянной поддержки доступны коммерческие услуги:

- Аудит соответствия 152-ФЗ и анализ пробелов
- Разработка кастомных YAML-конфигураций для вашей организации
- Интеграция с существующими системами (CMDB, тикетинг, HR)
- Обучение вашей команды практикам «Compliance as Code»
- Постоянная поддержка и обновления

Посетите [run-as-daemon.ru](https://run-as-daemon.ru) или свяжитесь через профиль GitHub для уточнения деталей.
